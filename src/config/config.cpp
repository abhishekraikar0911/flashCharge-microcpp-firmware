/**
 * @file Config.cpp (stub)
 * @brief Configuration implementations
 *
 * Note: These are stub implementations that compile.
 * Configuration values are defined in headers.
 */

// Stub implementations for config
